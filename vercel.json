{
  "version": 2,
  "buildCommand": "npx prisma generate --schema=./prisma/schema.prisma && cd frontend && npm install && npm run build",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.js"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
